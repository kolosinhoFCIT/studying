const storeName = "ATB";
let storeDesctiption = {
    budget: 10000,
    employees: ["Alex", "Billy", "Andy"],
    products: {"pen": 100, "pencil": 50},
    open: true
};

console.log(storeDesctiption.employees[0], storeDesctiption.products['pen'])
